server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/joceano/microservices-java-config
          default-label: main
          search-paths:
            - 'greeting-service*'
          #username:
          #password: